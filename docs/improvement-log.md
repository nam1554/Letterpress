# 개선 루프 기록

설계: `docs/superpowers/specs/2026-07-29-stability-improvement-loop-design.md`

베이스라인 (`5aee73a`): vitest 57 pass / 3 skip · tsc 0 · lint 0.

## 백로그

| 순위 | 축 | 상태 |
|---|---|---|
| 1 | 런타임 견고성 (`runner.ts`, `store.ts`) | ✅ R1 |
| 2 | 스트림 (SSE `events`) | ✅ R2 |
| 3 | 품질 게이트 (`acceptance.ts` + 자동 보수 경로) | ✅ R3 |
| 4 | API 경계 (라우트 13개) | ✅ R4 |
| 5 | UX 완성도 | ✅ R5 |
| 6 | E2E (mock 프로바이더) | 대기 |

### 관찰됐지만 고치지 않은 것

- `pnpm build`가 경고 1건을 낸다 (R5에서 처음 확인, R4 시점에도 동일하게 존재).
  `next.config.ts ← lib/verify.ts ← app/api/jobs/[id]/route.ts` 경로에서
  Turbopack NFT가 프로젝트 전체를 추적한다 — `lib/verify.ts`가 모듈 최상위
  경로에서 fs를 쓰기 때문. 빌드는 성공하며 런타임 영향은 없다. 고치려면
  파일 구조를 손봐야 해 자율 라운드 범위 밖으로 남긴다. (낮음)

- `store.ts` `live.reconciling`에 추가된 id가 제거되지 않는다. 같은 프로세스
  안에서 잡이 다시 stale이 되는 경로가 실제로는 없어 보여 보류. (낮음)
- 잡 id는 `randomUUID().slice(0, 8)` — 수만 건 규모에서만 충돌 위험. (낮음)
- 서버 재시작 직후 `STALE_GRACE_MS`(10초) 안에 잡 상세 페이지를 열면, SSE가
  잡 상태를 다시 읽지 않아 "실행 중"에서 멈춘 화면이 될 수 있다. 창을 벗어난
  뒤 접속하면 정상 복구되므로 보류. (낮음, 창 10초)

## 라운드 기록

### R1 — 런타임 견고성 (`c04614b`..)

발견 4건, 전부 수정. 테스트 +5 (57 → 62 pass).

| # | 결함 | 증상 | 수정 |
|---|---|---|---|
| 1 | `live.ts` — 신호 종료 시 정리 안 됨 | CLI는 `detached`라 포그라운드 그룹 SIGINT를 못 받고, SIGINT/SIGTERM은 `'exit'`을 발생시키지 않는다 → Ctrl-C 후 에이전트가 고아로 남아 토큰 소모 | `abortAllForShutdown()` 추출 + SIGINT/SIGTERM 훅. 다른 핸들러가 있으면 종료는 그쪽에 위임 |
| 2 | `store.ts` `readEvents` | 크래시로 마지막 줄이 잘리면 `catch`가 **전체 로그**를 버려 UI·SSE 리플레이가 통째로 빔 | 줄 단위 파싱 — 깨진 줄만 건너뜀 |
| 3 | `store.ts` `appendEvent` | 디스크 오류나 구독자(닫힌 SSE) 예외가 러너로 전파돼 실행 중인 잡을 죽임 | 쓰기·통지 모두 최선 노력. 구독자 집합은 복사본을 순회 |
| 4 | `runner.ts` `startJob` | 준비 단계 throw 시 컨트롤러·타이머 누수 → `runningJobCount()`가 영구히 부풀어 **이후 모든 작업이 429로 거부**되고 해당 잡은 삭제도 불가 | 준비 구간을 가드하고 실패 시 전역 상태 롤백 후 rethrow |

### R2 — 스트림 (SSE) (`943eb2b`..)

발견 1건 수정 + 라우트 테스트 인프라 확보. 테스트 +3 (62 → 65 pass).

- **결함**: 이탈 감지(`req.signal`) 등록이 리플레이·상태 전송이 **전부 끝난 뒤**에
  있었다. 리플레이는 파일 읽기를 기다리는 구간이라 그 사이 탭이 닫히는 일이
  흔한데, 그러면 등록 코드에 도달하지 못해 해당 연결의 구독이 정리되지 않고
  남는다. 잡이 이미 끝난 뒤라면 깨울 이벤트도 없어 영구 누수.
- **수정**: 이탈 감지를 구독보다 앞으로 옮기고, 이미 abort된 요청은 즉시 반환.
  리플레이·상태 전송의 `enqueue`를 전부 가드하고 `cancel()` 정리 경로를 추가.
- **인프라**: `vitest.config.ts`에 `@/` 별칭 추가 — 라우트 핸들러를 직접
  import해 테스트할 수 있게 됐다 (R4 API 경계 라운드의 전제).
- 리플레이/라이브 경계 중복 제거(`lastSeq`)와 종료 시 스트림 닫힘도 테스트로
  고정했다 — 기존 동작은 정확했다.

### R3 — 품질 게이트 (`22c9bd0`..)

발견 2건, 전부 수정. 테스트 +4 (65 → 69 pass).

- **결함 1 (높음): 이전 실행의 검증 결과가 이번 실행의 증거로 통했다.** 게이트는
  "PASS 여부와 무관하게 검증을 실행한 사실은 요구한다"를 계약으로 내걸었지만,
  edit 잡은 원본 workDir을 통째로 복사해 오고 resume은 같은 workDir을
  재사용한다 — 두 경우 모두 `verify.json`과 증거 이미지가 이미 자리에 있어
  계약이 공허했다. 재현 테스트에서 **아무것도 생성하지 않은 edit 잡이
  `succeeded`로 보고**됐고, UI에는 원본을 설명하는 PASS가 표시된다.
  → `AcceptanceOptions.freshSince`를 추가하고 러너가 이번 시도의 시작 시각을
  넘긴다. 그보다 오래된 `verify.json`은 별도 메시지로 실패 처리되며, 그 문구가
  자동 보수 프롬프트에 그대로 실려 에이전트가 재검증하도록 유도한다.
- **결함 2 (낮음): 0바이트 증거물이 `existsSync`를 통과했다** — compare.py가
  쓰다 죽으면 그렇게 남는다. → 크기 0을 없는 것으로 처리.

### R4 — API 경계 (`7bd2c6d`..)

경계 테스트 16건 신설, 발견 1건 수정. 테스트 +16 (69 → 85 pass).

라우트 13개에 테스트가 하나도 없었다. 경계 동작(잘못된 id, 상태 충돌,
경로 트래버설, 깨진 바디, 동시 실행 한도)을 스위트로 고정했고 **16건 중
15건은 이미 정확했다** — 검증 로직 자체는 튼튼했다.

- **결함 (중): `preview` 라우트가 params를 두 번 디코딩했다.** Next는 라우트
  params를 이미 퍼센트 디코딩해 넘긴다 — 실제 dev 서버로 확인했다(이름이
  literally `a%20b.png`인 파일이 `a%2520b.png`로 도달 불가 = Next가 디코딩).
  그래서 `100%.png` 같은 이름은 `decodeURIComponent`가 URIError를 던져 **500**,
  이름에 `%`가 든 파일은 **어떤 URL로도 열 수 없었다**. → 잉여 디코딩 제거.
  실제 서버 재확인: 500 → 200, 도달 불가 → 200, 기존 파일 회귀 없음,
  트래버설은 그대로 400.

### R5 — UX 완성도 (`ecb6138`..)

발견 2건, 전부 수정. 테스트 +7 (85 → 92 pass).

- **결함 1 (중): 실패한 요청이 아무 반응 없이 삼켜졌다.** 변경 요청 14곳이 전부
  `(await res.json()).error` 패턴이었다. 서버가 JSON이 아닌 응답(라우트의
  처리되지 않은 예외 → 500 에러 페이지)을 주거나 아예 닿지 않으면 그 자리에서
  예외가 나고, 사용자에게는 **버튼이 고장 난 것처럼 보인다** — 토스트도, 에러
  문구도 없다. R1에서 `startJob`이 준비 실패 시 throw하도록 바꾸면서 도달
  가능성이 더 커졌다. → `app/lib/request.ts`의 `requestJson`/`sendJson`으로
  통일. 절대 throw하지 않고 네트워크 실패·비-JSON 응답·API 에러를 각각 사람이
  읽을 수 있는 문구로 돌려준다. 호출부 14곳 전환.
- **결함 2 (중): 변환 시작·수정 요청에 더블클릭 가드가 없었다.** 제출 중 재진입을
  막지 않아 클릭 한 번이 에이전트 실행(10~25분, 실토큰)을 하나 더 만들 수 있었다.
  → `createAndGo`/`requestEdit`에 재진입 가드 추가.
