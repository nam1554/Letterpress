/**
 * 페이지 폭의 단일 출처.
 *
 * 680px였다. 1512px 화면에서 좁은 리본 하나가 빈 들판에 떠 있는 꼴이었고,
 * 히스토리 표는 열이 줄마다 흔들릴 만큼 좁았다. 920px면 표가 정렬될 공간이
 * 생기고, 긴 Figma URL 입력에도 오히려 유리하다.
 */
export const PAGE_WIDTH = 920;

/**
 * 본문 산문의 최대 폭 — 넓은 컨테이너에서 줄이 너무 길어지는 것을 막는다.
 *
 * px인 이유: `ch`는 폰트의 "0" 글리프 폭 기준이라 한글(전각에 가까운 폭)에는
 * 맞지 않는다. `62ch`로 뒀더니 실제로는 한글 약 30자에서 줄이 바뀌어 문단이
 * 의도보다 훨씬 좁게 접혔다.
 */
export const PROSE_WIDTH = 620;
